# Task Completion

- For file-promotion changes, validate source and destination YAML counts, names, and checksums; inspect `git status` and `git diff --stat`.
- For Python/shell changes, run the script's cheapest syntax or dry-run mode before wider GPU workloads.
- For kernel logic changes, additionally perform the applicable Tensile library-create or rocBLAS build/dispatch validation when the task requires executable proof.